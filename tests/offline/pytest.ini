[pytest]
pythonpath = . ../../src/aiops_agent_monitor
addopts = -m "not live and not stack" -p no:cacheprovider
markers =
    live: requires a real LLM API key (disabled by default, run with -m live)
    stack: requires the chapter-6 stack running (make all), run with -m stack
